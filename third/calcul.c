#include <unistd.h>
#include <stdio.h>


int main(void)
{
    int a;
    int b;
    char op;

    printf("\t CAL \t \n");

    scanf("%d %c %d", &a , &op, &b);

    if (op == '\'')
        printf();
}